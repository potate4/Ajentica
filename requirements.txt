# --- Web framework ---
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.30.0,<1.0

# --- Agent framework + LLM abstraction ---
# CrewAI uses LiteLLM internally, which gives us a single call signature
# across OpenAI, Anthropic, Gemini, OpenRouter, Ollama, Groq, Together, vLLM, etc.
crewai>=0.80.0,<1.0

# --- Vector store + embeddings ---
chromadb>=0.5.0,<0.6.0
sentence-transformers>=3.0.0,<4.0

# --- Config & utilities ---
pydantic>=2.10.0,<3.0
pydantic-settings>=2.5.0,<3.0
python-dotenv>=1.0.0
gitpython>=3.1.40,<4.0
